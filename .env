# 사람인 로그인 정보
SARAMIN_USERNAME=todenny
SARAMIN_PASSWORD=Lee283600

# 검색 조건 (여러 키워드는 쉼표로 구분)
SEARCH_KEYWORDS=바이오,생명공학,제약,의료기기
LOCATION=서울
JOB_TYPE=정규직

# 지원 설정
MAX_APPLICATIONS_PER_DAY=10
MAX_PAGES=5

# 대기 시간 설정 (초)
MIN_DELAY_BETWEEN_APPLICATIONS=30
MAX_DELAY_BETWEEN_APPLICATIONS=60

# 브라우저 설정
HEADLESS=false

# 로그 설정
LOG_LEVEL=INFO
LOG_FILE=saramin_bot.log

# PostgreSQL 연결 정보 (운영 환경에서 반드시 설정)
DATABASE_URL=postgresql://postgres:mysecret@localhost:5432/saramjob

# 데이터베이스 설정 (이전 버전 호환, PostgreSQL 사용 시 무시)
DB_FILE=applications.db
